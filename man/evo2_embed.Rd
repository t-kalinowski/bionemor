% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-adapter-evo2-predict.R
\name{evo2_embed}
\alias{evo2_embed}
\title{Extract Evo 2 sequence embeddings}
\usage{
evo2_embed(
  object,
  newdata,
  compute = NULL,
  layer = "last",
  pool = c("mean", "max", "first", "last", "none"),
  strand = c("forward", "reverse", "both"),
  batch_size = 1L,
  normalize = c("dna", "none"),
  control = evo2_inference_control(),
  output = NULL,
  name = NULL,
  async = FALSE
)
}
\arguments{
\item{object}{An Evo 2 model with an explicit checkpoint.}

\item{newdata}{Sequences or a FASTA path.}

\item{compute}{A BioNeMo compute descriptor. \code{NULL} uses the descriptor
attached by \code{\link[=evo2_model]{evo2_model()}} or a previous fine-tuning run.}

\item{layer}{\code{"last"} or a one-based decoder layer. R layer \code{1} maps to
upstream layer \code{0}; \code{"last"} maps to upstream \code{-1}.}

\item{pool}{Pooling across non-padding sequence positions. \code{"mean"} and
\code{"max"} aggregate values; \code{"first"} and \code{"last"} select an endpoint. With
\code{pool = "none"}, \code{output} is required and \code{strand = "both"} is not
supported.}

\item{strand}{Score the supplied sequence, its reverse complement, or both.}

\item{batch_size}{Prediction micro-batch size.}

\item{normalize}{Sequence normalization mode. With \code{normalize = "none"},
reverse-strand operations require uppercase IUPAC DNA.}

\item{control}{Controls from \code{\link[=evo2_inference_control]{evo2_inference_control()}}.}

\item{output}{For pooled embeddings, an optional prefix for the compressed
float32 data (\code{.f32.gz}) and JSON metadata (\code{.json}). For unpooled
embeddings, a required Parquet output path.}

\item{name}{Optional run name.}

\item{async}{Whether to return before completion.}
}
\value{
With \code{async = FALSE}, an \code{evo2_embeddings} numeric matrix for
pooled output or a \code{BioNeMoArtifact} for unpooled output. With
\code{async = TRUE}, a \code{BioNeMoJob}.
}
\description{
Extract Evo 2 sequence embeddings
}
\details{
Embeddings currently require \code{context_parallel_size = 1}. With a
pooling rule, token embeddings are pooled independently for each strand.
With \code{strand = "both"}, the forward and reverse pooled vectors are then
averaged. The returned matrix preserves input IDs as row names and names
its columns \code{dim_1}, \code{dim_2}, and so on.

Pooled embedding files store little-endian, row-major float32 values. R
reads these values into its native double-precision numeric matrix.

With \code{pool = "none"}, the result is a Parquet artifact with columns \code{id}
(string), \code{position} (int64), \code{embedding} (list of doubles), and \code{strand}
(string). Unpooled output requires \code{output} and does not support
\code{strand = "both"}.
}
\examples{
\dontrun{
compute <- bionemo_compute(recipe = evo2_recipe(), workspace = "~/evo2-work")
compute <- bionemo_install(compute)
model <- evo2_model("7b", compute)

embeddings <- evo2_embed(
  model,
  c(reference = "ACGTACGT", variant = "ACGTTCGT"),
  pool = "mean",
  strand = "both"
)
dim(embeddings)
embeddings[, 1:4, drop = FALSE]
}

}
\references{
\href{https://github.com/NVIDIA-BioNeMo/bionemo-recipes/blob/e8e7f597363c3b6dcc26f9b51fe683dd7f282f9e/recipes/evo2_megatron/README.md#batch-sequence-scoring-predict_evo2}{BioNeMo Recipes embedding extraction}
}
