% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-adapter-esm2.R
\name{esm2_embed}
\alias{esm2_embed}
\title{Extract pooled ESM-2 protein embeddings}
\usage{
esm2_embed(
  object,
  newdata,
  compute = NULL,
  output = NULL,
  name = NULL,
  async = FALSE
)
}
\arguments{
\item{object}{An ESM-2 model descriptor from \code{\link[=esm2]{esm2()}} or \code{\link[=esm2_model]{esm2_model()}}.}

\item{newdata}{A character vector of protein sequences, an \code{XStringSet}, a
data frame with a \code{sequence} column, or a FASTA path. Named inputs retain
their names as matrix row names.}

\item{compute}{A BioNeMo compute descriptor using \code{\link[=esm2_recipe]{esm2_recipe()}}. \code{NULL}
uses the compute target attached by \code{\link[=esm2_model]{esm2_model()}}.}

\item{output}{Optional path for the portable JSONL result. Container outputs
must be inside the compute workspace.}

\item{name}{Optional durable run name.}

\item{async}{Whether to return a \code{BioNeMoJob} before completion.}
}
\value{
A numeric matrix with class \code{esm2_embeddings}, or a \code{BioNeMoJob}
when \code{async = TRUE}.
}
\description{
\code{esm2_embed()} runs the ESM-2 model through the package-pinned native
Transformers and Transformer Engine runtime. It returns one last-token,
L2-normalized embedding row per input protein. Model weights are downloaded
from the model's pinned Hugging Face revision on first use and cached below
the compute workspace.
}
\details{
Use the embedding rows for sequence similarity, clustering, or as features
in downstream R models. They are model representations, not measurements of
protein function. ESM-2 currently requires \code{gpus = 1}. You may supply
multiple proteins; the runtime processes them one at a time on the selected
GPU to preserve their bidirectional attention boundaries.
}
\examples{
\dontrun{
compute <- bionemo_compute(
  recipe = esm2_recipe(),
  engine = "container",
  workspace = "~/bionemor-workspace"
)
compute <- bionemo_install(compute)
model <- esm2_model("8m", compute)
esm2_embed(model, c(reference = "MKT", variant = "MNT"))
}
}
