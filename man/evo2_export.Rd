% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-checkpoint.R
\name{evo2_export}
\alias{evo2_export}
\title{Export an Evo 2 checkpoint}
\usage{
evo2_export(
  model,
  path,
  format = "vortex",
  strip_optimizer = TRUE,
  compute,
  overwrite = FALSE,
  async = FALSE
)
}
\arguments{
\item{model}{An Evo 2 model bound to a dense MBridge checkpoint.}

\item{path}{Destination \code{.pt} path.}

\item{format}{Export format. Version 1 supports \code{"vortex"}.}

\item{strip_optimizer}{Whether to create a weights-only MBridge intermediate.}

\item{compute}{A compute specification.}

\item{overwrite}{Whether to replace an existing export.}

\item{async}{Whether to return a running job.}
}
\value{
A Vortex \code{BioNeMoCheckpoint}, or a \code{BioNeMoJob} when
\code{async = TRUE}.

The upstream exporter writes a shared \code{config.json} beside the \code{.pt} file,
so each destination directory may contain only one Vortex checkpoint.
The checkpoint inspector selects the Transformer Engine key mapping and
adds \code{--no-te} for a non-TE MBridge checkpoint. An unidentified key layout
is rejected before export.
}
\description{
Export an Evo 2 checkpoint
}
