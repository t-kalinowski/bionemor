% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-setup.R
\name{bionemo_install_plan}
\alias{bionemo_install_plan}
\title{Inspect how a BioNeMo recipe runtime will be installed or verified}
\usage{
bionemo_install_plan(compute)
}
\arguments{
\item{compute}{A BioNeMo compute descriptor from \code{\link[=bionemo_compute]{bionemo_compute()}}.}
}
\value{
A \code{BioNeMoSetupPlan} with \code{target = "install"}, the original compute
descriptor, the plan path, ordered command steps, and \code{executed = FALSE}.
}
\description{
\code{bionemo_install_plan()} constructs the full default installation or
verification plan for \code{compute}. It does not run its commands, pull or build
an image, probe a GPU, or submit a Slurm job. Use it to review the selected
recipe revision, image inputs, runtime commands, and working directories
before changing the execution environment. \code{\link[=bionemo_install]{bionemo_install()}} follows this
contract but can skip an existing image build or optional pull according to
its arguments and the current image state.
}
\section{Plans for each execution environment}{


The plan depends on the backend and engine in \code{compute}:
\itemize{
\item Local/container with the verified package recipe includes steps to fetch
the exact recipe revision, verify the locked Dockerfile and base-image
digest, build the deterministic image with the package helper, resolve its
image ID, and probe the helper and all supported recipe commands.
\item Local/container with an explicit prebuilt image includes image-ID and label
checks followed by the same runtime probes. It does not include a rebuild.
\item Local/external and both Slurm engines include verification probes only.
Those environments are managed outside bionemor. A Slurm/container plan
assumes an existing Apptainer image.
}

The plan describes the complete default installation check: inference,
training, and checkpoint-conversion commands are all included. To diagnose
one operation group after installation, use the \code{target} argument of
\code{\link[=bionemo_doctor]{bionemo_doctor()}}.
}

\section{Inspecting a plan}{


Printing a plan lists its ordered step IDs and purposes. \code{as.data.frame()}
exposes one row per step with these columns:
\itemize{
\item \code{step}: one-based execution order.
\item \code{id}: stable, machine-readable step name.
\item \code{purpose}: short description of the check or change.
\item \code{command}: rendered shell command with known credentials redacted.
\item \code{cwd}: working directory, also credential-redacted.
\item \code{expected}: list-column of structured values that the step must verify.
}

Plan inspection is not a substitute for installation: image digests and
live helper capabilities are resolved by \code{\link[=bionemo_install]{bionemo_install()}} and returned on
its updated compute descriptor.
}

\examples{
# Planning an external runtime is offline; the commands need not yet exist.
compute <- bionemo_compute(
  engine = "external",
  workspace = file.path(tempdir(), "bionemor-plan-example")
)
plan <- bionemo_install_plan(compute)
plan

steps <- as.data.frame(plan)
steps[c("step", "id", "purpose")]

}
\seealso{
\code{\link[=bionemo_compute]{bionemo_compute()}}, \code{\link[=bionemo_install]{bionemo_install()}}, \code{\link[=bionemo_doctor]{bionemo_doctor()}}
}
