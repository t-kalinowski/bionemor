% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bionemor-package.R
\docType{package}
\name{bionemor-package}
\alias{bionemor}
\alias{bionemor-package}
\title{Run biological foundation-model workflows from R}
\description{
\code{bionemor} provides R interfaces to selected biological foundation-model
assets and runtimes published through NVIDIA BioNeMo Recipes. It supports Evo
2 DNA workflows and ESM-2 protein embeddings through a shared sequence-input,
compute, durable-job, provenance, and R-result lifecycle.
}
\details{
Installing the R package and inspecting its model registries do not require a
GPU. Preparing weights, inference, preprocessing, and fine-tuning require a
supported CUDA-capable NVIDIA GPU; there is no CPU fallback. The
package-managed local runtime also requires Linux, Docker, and NVIDIA
Container Toolkit. A remote
Linux GPU from a provider such as Brev can be used when no local GPU is
available.
}
\section{How the pieces fit together}{


A model descriptor identifies a family, variant, configuration, and optional
checkpoint without loading weights into R. A recipe describes the pinned
family-specific source, dependencies, and commands. A compute descriptor
combines that recipe with a workspace, backend, runtime engine, and GPU
resources; a container is one engine selected by compute. A workflow is one
operation, and a durable job records its request, state, logs, outputs, and
provenance so it can be reopened in another R session.
}

\section{Start here}{

\itemize{
\item \code{\link[=bionemo_workflows]{bionemo_workflows()}} lists installed model-family workflows.
\item \code{\link[=bionemo_compute]{bionemo_compute()}} describes the selected recipe, GPU runtime, and
workspace.
\item \code{\link[=bionemo_install]{bionemo_install()}} prepares or checks that runtime.
}
}

\section{Evo 2 DNA workflows}{

\itemize{
\item \code{\link[=evo2_models]{evo2_models()}} lists the packaged model registry, and \code{\link[=evo2_model]{evo2_model()}}
prepares a checkpoint and binds it to compute.
\item \code{\link[=evo2_generate]{evo2_generate()}} extends DNA prompts.
\item \code{\link[=evo2_score]{evo2_score()}} scores complete sequences.
\item \code{\link[=evo2_profile]{evo2_profile()}} writes per-position log probabilities.
\item \code{\link[=evo2_embed]{evo2_embed()}} returns pooled embeddings or writes positional embeddings.
\item \code{\link[=evo2_dataset]{evo2_dataset()}}, \code{\link[=evo2_preprocess]{evo2_preprocess()}}, and \code{\link[=evo2_finetune]{evo2_finetune()}} describe and
preprocess training data, then fine-tune a model.
\item \code{\link[=evo2_checkpoint]{evo2_checkpoint()}} and \code{\link[=evo2_export]{evo2_export()}} convert and export checkpoints.
}
}

\section{ESM-2 protein embeddings}{

\itemize{
\item \code{\link[=esm2_models]{esm2_models()}} lists pinned NVIDIA ESM-2 checkpoints.
\item \code{\link[=esm2_model]{esm2_model()}} binds a checkpoint to an ESM-2 recipe runtime.
\item \code{\link[=esm2_embed]{esm2_embed()}} returns last-token, L2-normalized protein embeddings.
}

Operations write durable requests, logs, state, and portable results below
the compute workspace. Use \code{\link[=bionemo_job]{bionemo_job()}} to reopen a run after the R session
ends and \code{\link[=job_result]{job_result()}} to retrieve its result.
}

\seealso{
Useful links:
\itemize{
  \item \url{https://t-kalinowski.github.io/bionemor/}
  \item \url{https://github.com/t-kalinowski/bionemor}
  \item Report bugs at \url{https://github.com/t-kalinowski/bionemor/issues}
}

}
\author{
\strong{Maintainer}: Tomasz Kalinowski \email{kalinowskit@gmail.com}

Authors:
\itemize{
  \item Tomasz Kalinowski \email{kalinowskit@gmail.com}
}

}
\keyword{internal}
