% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-checkpoint.R
\name{evo2_model}
\alias{evo2_model}
\title{Prepare a recommended Evo 2 model}
\usage{
evo2_model(size = "7b", compute, path = NULL)
}
\arguments{
\item{size}{Canonical Evo 2 model name or a known upstream alias.}

\item{compute}{A compute specification.}

\item{path}{Optional checkpoint destination or reuse location. Relative
paths are resolved below the compute workspace. \code{NULL} uses a
revision-qualified path below \verb{checkpoints/}.}
}
\value{
An S7 \code{Evo2Model} with a \code{BioNeMoCheckpoint} attached.
}
\description{
\code{evo2_model()} prepares or reuses the registry-recommended dense MBridge
checkpoint and returns an Evo 2 model with that checkpoint and \code{compute}
attached. Inference and fine-tuning functions use that compute descriptor by
default. The function does not install or diagnose the runtime. Preparation
is synchronous.
}
\details{
With \code{path = NULL}, the destination below the compute workspace is keyed by
the canonical model name, source revision, and recipe revision. An explicit
\code{path} changes only that destination or reuse location; it does not change
the registered source. Existing incomplete or mismatched destinations are
rejected without being overwritten.
}
\examples{
\dontrun{
compute <- bionemo_compute(recipe = evo2_recipe(), workspace = "~/evo2-work")
model <- evo2_model("7b", compute)

# The prepared model remembers where it runs.
evo2_score(model, c(example = "ACGTACGT"))
}
}
\seealso{
\code{\link[=evo2]{evo2()}}, \code{\link[=evo2_checkpoint]{evo2_checkpoint()}}, \code{\link[=evo2_models]{evo2_models()}}
}
