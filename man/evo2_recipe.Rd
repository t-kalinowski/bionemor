% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-adapter-evo2-recipe.R
\name{evo2_recipe}
\alias{evo2_recipe}
\title{Describe the Evo 2 BioNeMo recipe}
\usage{
evo2_recipe(
  revision = "recommended",
  repository = NULL,
  base_image = NULL,
  allow_mutable = FALSE
)
}
\arguments{
\item{revision}{BioNeMo Recipes revision. \code{"recommended"} uses the package
lock. Otherwise, supply a full commit SHA unless \code{allow_mutable = TRUE}.}

\item{repository}{BioNeMo Recipes repository URL. \code{NULL} uses the lock.}

\item{base_image}{Base image reference. \code{NULL} uses the lock.}

\item{allow_mutable}{Whether \code{revision} may be a branch or tag.}
}
\value{
A \code{BioNeMoRecipe} descriptor.
}
\description{
\code{evo2_recipe()} identifies the versioned BioNeMo Recipes environment used
for Evo 2 checkpoint preparation, inference, and fine-tuning. It is offline:
it reads the lock bundled with bionemor without cloning source, pulling an
image, or starting a runtime.
}
\details{
Use the result as the required \code{recipe} argument to \code{\link[=bionemo_compute]{bionemo_compute()}}, then
use \code{\link[=bionemo_install]{bionemo_install()}} to build or verify the runtime. Changing a locked
source field creates an unverified descriptor. Unverified recipes require an
externally managed runtime or an explicit prebuilt image.
}
\examples{
recipe <- evo2_recipe()
recipe

}
\seealso{
\code{\link[=bionemo_compute]{bionemo_compute()}}, \code{\link[=bionemo_install]{bionemo_install()}}
}
